using UiPath.CodedWorkflows;
using System;

namespace POC_Reconcile_KPC
{
    public class ConnectionsManager
    {
        public ConnectionsManager(ICodedWorkflowsServiceContainer resolver)
        {
        }
    }
}